# frontend-b (log panel)

React component library consumed by `frontend-a`. Storybook documents components. Runtime image serves the Storybook static build.

| Area        | Notes                                      |
| ----------- | ------------------------------------------ |
| Build       | Vite library mode + `vite-plugin-dts`      |
| Docs        | Storybook 8 + `@storybook/react-vite`      |
| Tests       | Vitest + Testing Library, 80% threshold  |
